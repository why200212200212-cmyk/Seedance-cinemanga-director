# Changelog

## [Unreleased]

- 新增按需加载的多人动作导演模块：FTR/TEAM身份图、ZONE/ACT空间拓扑、行动令牌、逐拍攻防幻觉、武术套路影视化、3D碰撞和真人特技安全边界。
- 新增武术与影视动作参考资料，整理IWUF套路/对练/集体项目结构、影视行业特技安全规范及动作生物力学研究；资料只提供动作结构与物理参考，不输出现实伤害教程。
- 新增多人动作扩展模板、动作专项质量检查、验收用例及黑金蓝模块介绍图 `multi-fighter-action-system.png`。
- 新增图片/视频定点修订闭环：稳定资产ID与Rxx版本账本、自然语言目标反查、修订单、生成前完整提示词预览、用户编辑/批准门槛、最小重生成单位和旧版保留机制。
- 新增依赖影响传播规则：角色板、场景板、分镜页、视频尾帧或结束状态变化只把相关下游标为 `REVIEW_REQUIRED`，未经用户批准不自动连锁付费生成。
- 精简 `SKILL.md` 常驻触发描述并将入口改为执行约束，保留原有剧情分析、角色注册、自适应分镜、双影视引擎、连续性质检与API执行主框架。
- 新增可复现的最小 Skill 打包脚本：发布包仅包含运行必需文件，不携带项目展示资产、维护文档、测试、真实 `.env` 或用户凭证。
- 新增 `doctor` 离线环境自检与用户主动开启的 `doctor --remote` 只读连通性检查；明确 API 始终由下载者在自己的 AI 宿主中接入，CI 不持有密钥、不执行远程测试。
- 补充 Codex、OpenClaw 与其他 AI 的宿主工具映射和权限边界，并增加最小包复现性、秘密不回显及只读自检测试。
- 新增视频任务 `list` 恢复命令：创建响应不确定时可按状态、任务ID或推理接入点找回已接收任务，避免盲目重发 POST 造成重复计费；并补充离线契约测试与运行文档。
- 修正“用胶片颗粒或镜头风格词处理背景扭曲”的误导建议，改为降低镜头复杂度、固定空间锚点和使用场景/运镜参考。
- 新增执行素材绑定清单，把角色ID、独立角色板、VID/分镜页、USE镜头、视频提示词图片编号与API content顺序收敛为同一可追溯合同；连续多条按VID重新生成临时图片序号。
- 统一四种分镜载体的4–15秒执行合同：每张执行板只服务一个VID，按时长、节拍与复杂度灵活选板；页头标总时长与15秒上限，每个USE镜头标起止时间和单镜时长，连续多条逐条分板并从0.0秒重新计时；
- 接入用户提供的16–22号参考资料：13字段分镜大表、局部映射案例、状态变化追踪、高难度运镜与Seedance模板、一镜到底实现指南与Seedance模板；知识库由16份扩展为23份；
- 新增案例隔离、高难度镜头可执行性闸门、真/伪一镜到底标注、伪一镜逐接缝连续性合同与独立生成4–15秒边界；模板只作按需改写骨架，不覆盖核心流程或当前平台合同；
- 逐项比对13–15号原资料与仓库版本，保留已核验的Seedance参数修正、Runway平台隔离和工具选型时效边界，避免回退为未校正数据；
- 全面审计 API 客户端、文档、参考知识库、视觉资产校验和 GitHub Actions；修复超时边界、轮询截止点、超长 `Retry-After`、异常响应解码与下载残留文件等问题；
- Seedance 2.0 便捷参考图请求新增官方 `role: reference_image`，并支持已授权可信人物素材的 `asset://asset-id` URI；
- 校正知识库残留的“4–16秒、最高2K”旧参数为当前官方“4–15秒、最高1080p”，并降低未核验竞品参数和数值运镜承诺的权重；
- Bandit 安全工作流改为发现问题即失败；结构校验新增工作流闭锁、PNG文件头/尺寸、乱码字符和过期平台参数检查；
- 扩展 API 离线回归测试，覆盖安全 URL、官方资产、有限重试、POST 不重试、轮询终态、超时、无效 JSON、下载与 dry-run。
- 将流程调整为剧情分析优先，再按需要选择标准故事板、九宫格、二十五宫格或智能宫格；
- 新增 Seedance 2.0 详细分镜图规范，包括阅读顺序、镜头序号、时间映射、摄影、角色、动作、声音、光影、转场和连续性标注；
- 新增同剧本角色面部差异化硬规则、两两区分检查和模板脸禁令；
- 为双胞胎、多胞胎、克隆体、替身及同一人物不同版本增加受剧情约束的相似例外；
- 同步更新单条/多条模板、质量清单、验收用例、结构校验与 README 安装地址。
- 接入用户提供的00–15号AI导演资料，形成16份可按需调用的分镜与运镜参考知识库；
- 新增知识库冲突优先级、平台时效边界、Seedance 2.0官方核验摘要和跨平台资料隔离规则；
- 在原第5步专项加载中接入知识库，不改变剧情分析优先、角色设计、状态账本、镜头取舍、路线规划和详细分镜图的既定顺序。
- 新增 `knowledge-routing-map.png` 与 `seedance-multimodal-binding.png` 两张文档装饰图，并放入README、架构说明和视觉资产索引的对应位置。

## 2026-07-15 — Quality hardening

- 新增零依赖火山方舟视频任务客户端，支持创建、查询、等待、取消、尾帧请求和安全下载；模型与推理接入点保持可配置。
- 新增 Codex、OpenClaw 与其他 AgentSkills 宿主的统一编排契约、API 配置文档、dotenv 示例和 dry-run 示例。
- 新增离线客户端单元测试与 CI 请求预检；CI 不持有密钥、不联网创建任务，也不会产生生成费用。
- 清理未配置且与本项目技术栈无关的商业安全扫描模板；保留 CodeQL、Bandit 与项目原生验证，并把检出动作更新到当前主版本。
- 新增 GitHub Actions，在 Python 3.10 与 3.13 上自动校验技能、文档链接和视觉资产；
- 将风格参考收缩为模式决策路由，避免3D与真人专项规则重复加载；
- 补充不可满足的单条时长冲突、合法换场、匹配剪辑、复杂度过载、模式隔离和平台能力未知等验收用例；
- 把 CI 工作流和关键边界用例纳入结构校验。

## 2026-07-15

- 合并更新包中的高清视觉资产、视觉展示文档与架构说明；
- 新增 Codex `agents/openai.yaml` 界面元数据；
- 新增3D影视级材质、动画、物理、虚拟摄影机和失败抑制规范；
- 新增真人影视级摄影机、曝光、景深、灯光、演员调度和现场状态规范；
- 新增提示词优先级、冲突消解、复杂度预算和最终压缩规则；
- 在单条与多条模板中加入摄影/物理合同、状态差分和状态账本；
- 扩展最终质量检查与本地结构校验。
- 加入中英双语非官方声明、Codex 安装路径、转场感知输出说明与概念图免责声明；
- 对所有 PNG 做内容哈希去重，并让校验器检查图片清单、断链与重复内容。

本项目遵循语义化版本方向维护。

## [0.1.1] - 2026-07-15

### Added

- 新增封面主视觉、技能总览、系统架构图、图标资产板、六镜分镜板、九宫格和二十五格示例图；
- 在 `README.md` 中加入视觉预览、系统架构总览、分镜板示例资源和文档资产区；
- 新增 `docs/visual-showcase.md` 说明图片在仓库中的最佳落位；
- 扩充 `assets/README.md`，明确每张图的用途与放置原则。

## [0.1.0] - 2026-07-15

### Added

- 初始 `SKILL.md` 导演系统；
- 精致3D国漫影视级与真人影视级双模式；
- 完整15秒单条模板；
- 连续多条4–15秒尾帧接力模板；
- 角色、空间、道具、光影和屏幕方向连续性规则；
- 对白时长与结尾余量校验；
- 导演级镜头语言参考；
- 原创输入、单条输出和连续输出示例；
- 无依赖本地结构校验器；
- 四张项目预览资产。
